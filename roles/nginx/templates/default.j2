# {{ ansible_managed }}
server {
    listen 80 default_server;
    listen [::]:80 default_server ipv6only=on;

    server_name _;
    server_name_in_redirect off;
{% if nginx_default_redirect_url %}
    return 301 {{ nginx_default_redirect_url }};
{% else %}
    return 404;
{% endif %}
}
